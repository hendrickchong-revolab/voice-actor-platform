{
  "name": "va-platform",
  "private": true,
  "scripts": {
    "check": "npm --prefix web run check",
    "dev": "bash scripts/dev.sh",
    "dev:nisqa": "USE_NISQA=1 bash scripts/dev.sh",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
